# dsh-modef bundle patch: inserts the composer model/effort slider row into
# the profile roster. Applied as a profile bundle layer via the
# `dsh.bundle.patch` manifest field.
- insert:
    - id: dsh-modef
      name: '@magiczerowxy/dsh-modef'
