/** Optional Angelina theme plugin, node half. Browser behavior ships via exports["./client"]. */

/** Host plugin body — no host-side behavior for this theme surface plugin. */
export function apply(): void {}
